type attributes = (string * string) list <json repr="object">

type cora_unchecked = {
    name: string;
    ?value: string option;
    ?repeat_id: string option;
    ?children: cora_unchecked list option;
    ?attributes: attributes option; 
}